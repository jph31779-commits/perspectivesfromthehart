# perspectivesfromthehart
creative side

## Preview

Open `index.html` in a browser, or run a simple local server from the project root:

```bash
python3 -m http.server 8000
# then visit http://localhost:8000
```

Files added:

- `index.html` — improved markup and accessibility
- `styles.css` — extracted styles
- `favicon.svg` — small site icon
